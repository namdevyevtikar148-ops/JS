// typeof array is object
let info=["shradha",121,89.9];
//collection of different elements
console.log(info[0][0]);
//retrives the first character of the string
// Arrays are mutable
let fruits=["mango","apple","banana"];
fruits[0]="pineapple";
console.log(fruits);
//push - adds at the end
//pop  - deletes from the end & returns the array
//unshift - add at the begining
//shift- deletes from the beginging
//indexf

//includes- returs true or false
let primary=["red","orange","blue"];
let secondary=["black","green","Yellow"];
console.log(primary.concat(secondary));
//reverse- reverse the array
// primary.reverse();

// slice-   copies portion of the array
let x=primary.slice();
console.log(x);
